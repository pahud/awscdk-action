# action.yml
name: 'CDK action'
description: 'CDK action'
inputs:
  command:  # id of input
    description: 'cdk subcommand'
    required: true
outputs:
    version:
        description: 'The version of CDK'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.command }}
